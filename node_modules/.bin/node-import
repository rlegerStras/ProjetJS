#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../node-import/bin/node-import" "$@"
  ret=$?
else 
  node  "$basedir/../node-import/bin/node-import" "$@"
  ret=$?
fi
exit $ret
